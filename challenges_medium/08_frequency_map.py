import os

os.system("cls" if os.name == "nt" else "clear")

# Frequency Map
# Take a string like "banana". Create a dictionary where the keys are the letters and the values are how many times that letter appears. { "b": 1, "a": 3, "n": 2 }.

word = "banana"

a = word.split()
